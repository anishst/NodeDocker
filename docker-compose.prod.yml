version: "3"
services: 
    nginx:
        ports: 
            # serve on port 80 in prod
            - "80:80"
    node-app: 
        build: 
            # location of dockerfile
            context: .
            args: 
                NODE_ENV: production        
        environment:
            - NODE_ENV=production
        command: node index.js